---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../config';

const posts = (await Astro.glob('./blog/*.{md,mdx}')).sort(
	(a, b) => new Date(b.frontmatter.pubDate).valueOf() - new Date(a.frontmatter.pubDate).valueOf()
);
---

<!DOCTYPE html>
<html lang="en">
<head>
  <BaseHead title={`Blog – ${SITE_TITLE}`} description={SITE_DESCRIPTION} />
</head>
<body>
  <Header />
  <main>
    <blockquote>🚧 Coming soon! Posts are migrating here but haven't yet arrived.</blockquote>
    <section>
      <h2>Quick links:</h2>
      <ul>
        {
          posts.map((post) => (
            <li>
              {/* <time datetime={post.frontmatter.pubDate}>
                {new Date(post.frontmatter.pubDate).toLocaleDateString('en-us', {
                  year: 'numeric',
                  month: 'short',
                  day: 'numeric',
                })}
              </time> */}
              <a href={post.url}>{post.frontmatter.title}</a>
            </li>
            ))
        }
        <li><a href="/2022">2022 Year in Review</a></li>
        <li><a href="/2021">2021: More Social, Less Media</a></li>
        <li><a href="/2020">2020 Year in Review</a></li>
        <li><a href="/2019">2019 Year in Review</a></li>
      </ul>
    </section>
    <p>
      <strong>For others, visit my <a href="/medium">Medium blog</a>.</strong>
    </p>
  </main>
  <Footer />
</body>
</html>
